function setup() {
  createCanvas(340, 340);
}


function draw() {
  var c = random(width);
  if (c < 170) {
  stroke(255, 204, 0);
  } else {
    stroke(255, 0, 0);
    }
  
  line(c, random(height), random(width), random(height));
  
}
